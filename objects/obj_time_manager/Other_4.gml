// Room Start — se ejecuta cada vez que el objeto entra en un room.
// Como obj_time_manager es persistent, su Create solo corre una vez.
// Este evento garantiza que la capa de colisión se oculte en todos
// los rooms siguientes al primero.
scr_hide_collision_layer();
